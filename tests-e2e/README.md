# tests-e2e (migrated)

This directory is a short-lived compatibility stub for developers who may still reference `tests-e2e/`.

All Playwright E2E tests have been migrated to `tests/e2e/` (preferred) to keep all tests under `tests/`.

Please update your references to `tests/e2e` and feel free to remove this stub after a short transition period.