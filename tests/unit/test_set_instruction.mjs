/* eslint-disable no-console, no-undef, no-unused-vars */
/* eslint-env node, browser */
import { test } from 'vitest';

// Long diagnostic moved to tests/scripts/test_set_instruction.mjs
// Excluded from unit runs; run the script directly from `tests/scripts/test_set_instruction.mjs`.

test.skip('test_set_instruction moved to tests/scripts (long diagnostic)', () => {
  // intentionally empty placeholder
});

