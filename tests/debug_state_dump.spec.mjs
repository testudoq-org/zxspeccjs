// This file moved to `tests-e2e/debug_state_dump.spec.mjs` to ensure Playwright
// E2E tests are not imported by the unit test runner (Vitest).
// Kept as a stub to avoid accidental execution under Vitest.

import { it, expect } from 'vitest';

it('stub placeholder test to prevent accidental Vitest import', () => {
  expect(true).toBe(true);
});